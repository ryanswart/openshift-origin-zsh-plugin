# openshift-origin-zsh-plugin
A few shortcuts to common openshift origin (oc) actions

# Usage
`podlogs {application-name} {since}` will find the latest pod for given application, and print its logs since the given time
